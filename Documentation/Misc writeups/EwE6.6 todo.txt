EwE todo
========

  Warning if there are no water cells on pre-run
  Hatchery stocking ff cannot be removed from stanza group -> Works OK in Georgia Strait. Joe?
V Pedigree not drawing in pedigree grid (but data is there?), not showing on grids
V In the MTI, the options to change the image are not showing up
V In the keystone index, the options to change the figure to include biomasses are not showing up 
V Group capacity model grid: use fixed column widths
  
V MPA dyn: export button needs tooltip
V MPA dyn: exported csv needs year + month, not just year
V MPA dyn: export CSV link opens file, not the folder
  MPA dyn: add 'all year', 'all fleets' columns to CSV file (or 'm*', 'fleet*') -> Colette?
  
  Connect ext data -> define ext data -> connection list does not refresh
  External data grid incremental updates, not total refresh
  
? How does RelPP layer work with multiple Producer groups?

V New movement parm in Ecospace: movement rel to cell fitness (not cap; prod as ratio of loss) 0 = default
V Ecospace dispersal grid: EatEffBad can be deleted from the UI? 
  Disable respiration warning for MS groups: non-feeding last stanza high mort -> resp warning
  Ecosampler: add means to select batch run start sample index
  
V Import Hake HSPEN Depth does not set depth limits correctly (40-1000m imports to limits 0-1m)
V Copy layer style does not work between env driver layers
V Navigator > Ecospace: move habitat foraging usage nav item up
  Sort out confusing terminology in Space: fn responses, env responses, habitat foraging responses
  Rewrite map? Double-buffering now can create out of memory errors. Perhaps simple fix will do? If not, manage zooming in code (not in WinForms)
V Add "Auto save settings" group box in Ecospace Parameters
  Ecowriter: remark column size to width, not size to content
  
? Bring Ecosim Nutrient loading + Production forcing per PP group to Ecospace: 
     - Add Nutrient loading layer
	 - Add RelPP layer per PP group
? Allow remarks on forcing & mediation functions
? Allow plug-in points on toolstrips (by identifying the class name of the grid / form that has a main toolbar, like quick editor)
? Hyperbolic function parameters are still defaults, is this correct?

  Add cStringUtils.ToParagraph, adding line breaks after a preferred no. of characters, and use this in tooltips